// Timestamps (in milisecs) for the background video to randomly start at..
var timestamps = [6000, 18000, 80200, 90200]
// the time interval the blur effect will apply on UI elements
var blurInterval = [94283, 633] //can wrap around