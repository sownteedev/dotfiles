import { Variable } from "astal";

/** Form thêm sự kiện Calendar (full-screen overlay trong app API) */
export const showEventForm = Variable(false);

/** Form thêm Google Task */
export const showTodoAddForm = Variable(false);
